import { Button } from "@heroui/react";

export default function Home() {
  return (
    <main className="flex items-center justify-center py-3">
      <Button className="max-w-xl rounded-full" fullWidth>
        Button
      </Button>
    </main>
  );
}
